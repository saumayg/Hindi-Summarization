We have dataset. Extract named entity from each sentence of the article
and create an extractive score for the sentence. Take first n sentences(hyperparameter)
and pass that shortened to another abstractive summarization model.